package q1;

/**
 * Class allowing the user to calculate the average of a list of integers with a single thread
 */
public class AlgorithmSingleThread extends Algorithm {

    public AlgorithmSingleThread() {
        generateList();
    }
}
